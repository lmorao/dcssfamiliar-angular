export class Weapon {
    // eg. "short_blades"
    category: string;
    name: string;
    damage: number;
    hit: string;
    speed: number;
    //one hand minimum size
    ohms: string;
    //two hand minimum size
    thms: string;
    damage_type: string;
    // change to drop
    prob: number;
}
